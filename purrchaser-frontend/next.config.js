/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['creative.nd.edu', 'u-mercari-images.mercdn.net', 'www.hypeandvice.com']
    },
}

module.exports = nextConfig
